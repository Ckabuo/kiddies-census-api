# Server Configuration
PORT=5001
NODE_ENV=development

# Database Configuration
MONGODB_URI=mongodb://localhost:27017/kiddies
# For MongoDB Atlas, use: mongodb+srv://username:password@cluster.mongodb.net/kiddies

# JWT Configuration
JWT_SECRET=your-secret-key-change-this-in-production-use-a-long-random-string
JWT_EXPIRES_IN=7d

# Email Configuration (for sending invitations)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password
# Note: For Gmail, you need to generate an "App Password" in your Google Account settings

# Frontend URL (for CORS and invite email links; set to Netlify URL in production)
FRONTEND_URL=http://localhost:3000

# Admin Seed Configuration (optional - for seed script)
# ADMIN_EMAIL=admin@church.org
# ADMIN_PASSWORD=admin123
# ADMIN_FIRST_NAME=System
# ADMIN_LAST_NAME=Administrator
# ADMIN_PHONE=08000000000
